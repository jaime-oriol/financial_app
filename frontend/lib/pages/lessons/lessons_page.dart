/// Lessons page. Ref: Solution Design, wireframe 4.
/// Modulos de educacion financiera, daily challenge, badges.
import 'package:flutter/material.dart';

class LessonsPage extends StatelessWidget {
  const LessonsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Lessons — coming soon'),
    );
  }
}
